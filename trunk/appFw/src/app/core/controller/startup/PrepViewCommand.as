package app.core.controller.startup {	import app.core.view.ApplicationView;	import app.core.view.mediators.ApplicationMediator;	import org.robotlegs.base.ContextEvent;	import org.robotlegs.mvcs.Command;	public class PrepViewCommand extends Command {		[Inject] 		public var event : ContextEvent;		public function PrepViewCommand() {		}		override public function execute() : void {			initMediators();		}		private function initMediators() : void {			mediatorMap.mapView(ApplicationView, ApplicationMediator);			log("Map application specific mediators!");		}	}}