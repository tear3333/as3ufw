package as3ufw.ui.layout.mangers {

	/**
	 * @author Richard.Jewson
	 */
	public class HBox extends Canvas {
		public function HBox(canvasWidth : Number, canvasHeight : Number, scrollPolicy : String = NONE, maskCanvas : Boolean = true) {
			super(canvasWidth, canvasHeight, scrollPolicy, maskCanvas);
		}
	}
}
