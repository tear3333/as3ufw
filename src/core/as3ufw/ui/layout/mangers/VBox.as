package as3ufw.ui.layout.mangers {

	/**
	 * @author Richard.Jewson
	 */
	public class VBox extends Canvas {
		public function VBox(canvasWidth : Number, canvasHeight : Number, scrollPolicy : String = NONE, maskCanvas : Boolean = true) {
			super(canvasWidth, canvasHeight, scrollPolicy, maskCanvas);
		}
	}
}
