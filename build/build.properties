# Build paths
FLEX_HOME = C:/dev/sdk/Flex4

# On a pc add .exe to these, on a mac you don't need the extension
asdoc.exe = asdoc.exe
compc.exe = compc.exe

#report output file
FlexUnitReport.dir=${basedir}/flexUnitReport
#test dir
Test.dir=${basedir}/testUnit

#Test Runner Dir
TestRunner.dir=${basedir}/test
TestRunnerSrc.dir=${TestRunner.dir}
TestRunnerLib.dir=${TestRunner.dir}
#Test Runner Dir
TestRunner.name=as3ufwTests